extends layout
block content
	.container
		.row
			h2 List of spookmarks
			table(width="500px")
				each d, index in spookmarks
					tr
						td= d.userID
						td= d.spookTitle
						td= decodeURIComponent(d.spookURL)
						td= d.spookDescription
						td= moment(d.spookTime).fromNow()